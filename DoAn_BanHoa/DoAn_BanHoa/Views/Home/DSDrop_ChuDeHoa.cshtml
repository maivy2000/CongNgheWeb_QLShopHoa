@using DoAn_BanHoa.Models;
@model List<CHUDE>
<div class="nav-item dropdown ">
    <a href="#" class=" nav-link dropdown-toggle text-black " role="button" data-toggle="dropdown">HOA THEO CHỦ ĐỀ
    </a>
    <ul class="dropdown-menu ">
        @foreach (var item in Model)
        {
        
            <li><a href="@Url.Action("HTTheoChuDe", "Home", new { @mcd = @item.MACHUDE })" class="dropdown-item text-black" >@item.TENCHUDE</a></li>
        }
    </ul>
</div>